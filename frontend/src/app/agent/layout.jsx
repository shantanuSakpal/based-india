import SecondaryNavbar from "@/components/SecondaryNavbar";

const Layout = ({ children }) => {
  return (
    <main className="">
      {/* <SecondaryNavbar /> */}
      {children}
    </main>
  );
};

export default Layout;
